import 'package:flutter/material.dart';

class SalaScreen extends StatelessWidget {
  const SalaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Sala (placeholder)')),
    );
  }
}
